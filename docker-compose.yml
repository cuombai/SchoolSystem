version: '3.8'
services:
  SchoolSystem:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      # Add environment variables here if needed
      # EXAMPLE_ENV: value
    restart: unless-stopped
